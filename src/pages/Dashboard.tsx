import { Typography } from "@mui/material";

export default function Dashboard() {
  return (
    <div>
      <Typography variant="h4">Bienvenido al panel principal 🚀</Typography>
    </div>
  );
}